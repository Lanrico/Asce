//这个文件是配合一些框架的js使用，因为框架的js必须要在html网页加载完之后再加载，而且chrome还不支持inline的js :(
$('#search_dropdown').dropdown();
$('.menu .item').tab();
$('.ui.checkbox')
  .checkbox()
;
$('select.dropdown')
  .dropdown()
;